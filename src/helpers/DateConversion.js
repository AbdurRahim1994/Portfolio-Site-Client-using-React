import moment from "moment";

export function yearDiff(d1, d2) {
    // var m = moment(d1);
    // var years = m.diff(d2, 'years');
    // m.add(-years, 'years');
    // var months = m.diff(d2, 'months');
    // //m.add(-months, 'months');
    // var days = m.diff(d2, 'days');
    // return  months
    // //return months === 0 ? years + " year " : years + " year " + months + " month "

    
}